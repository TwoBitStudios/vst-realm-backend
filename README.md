# VST Hub Backend

## Development

Open a terminal:
`poetry install --with dev`
`docker-compose up`

Open a new terminal:
`fastapi dev core/main.py`

To kill the MongoDB Container:
`docker-compose down`
